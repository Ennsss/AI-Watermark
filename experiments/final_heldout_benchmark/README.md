# Final held-out benchmark output directory

This directory is intentionally empty of results at methodology freeze time.
The guarded final runner will later create the frozen configuration snapshot,
environment and identity metadata, paired per-sample/condition results,
fidelity and computational measurements, statistical tests, effect sizes, and
the final summary. No placeholder numeric result files are committed.
